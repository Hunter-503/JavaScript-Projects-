function changeColor(newColor) {
    var elem = document.getElementById('cap');
    elem.style.color = newColor;
  }

  function myFunction() {
    var paragraph = "JavaScript doesnt make sense";
    paragraph += " I hope I don't fail this course!";
    document.getElementById("isuck").innerHTML = paragraph;
}